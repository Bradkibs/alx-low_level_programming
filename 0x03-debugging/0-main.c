#include "main.h"

/**
 * main - tests the function that prints if the integer is positive or negative
 *
 * Return: 0 when successful
 *
 */
int main(void)
{
	int integer;

	integer = 0;

	positive_or_negative(integer);
	return (0);
}
